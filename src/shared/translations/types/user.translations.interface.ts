export interface UserTranslations {
  profile: {
    follow: {
      delete: string;
      put: string;
    };
    playlists: {
      title: {
        all: string;
        public: string;
      };
    };
    title: string;
  };
}
