export interface TrackTranslations {
  action: {
    add: {
      success: string;
      title: string;
    };
    delete: {
      success: string;
      title: string;
    };
  };
  title: string;
}
