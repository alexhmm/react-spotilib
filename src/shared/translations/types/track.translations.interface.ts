export interface TrackTranslations {
  action: {
    add_to_playlist: {
      success: string;
      title: string;
    };
    favorite: {
      success: string;
      title: string;
    };
    unfavorite: {
      success: string;
      title: string;
    };
  };
  title: string;
}
